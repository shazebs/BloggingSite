# localhost MySQL
#spring.datasource.url=jdbc:mysql://localhost:3306/blogsite
#spring.datasource.username=root
#spring.datasource.password=root

# JawsDB Heroku
spring.datasource.url=${DB_CONNECTION_STRING}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# for MySQL 
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Azure SQL Server
#spring.datasource.url=${bloggingsite_db_url}
#spring.datasource.username=${bloggingsite_db_username}
#spring.datasource.password=${bloggingsite_db_password}
#spring.sql.init.mode=always

logging.level.root=INFO
logging.level.com.gcu=TRACE